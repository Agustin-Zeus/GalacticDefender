using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class kk : MonoBehaviour
{
    /*


    private PoolProyectilesBoss2 pool;

    private PoolMisil_boss2 Pool;
    private void OnTriggerEnter2D(Collider2D other)
    {
        // Desactiva el objeto con el que colisiona
        other.gameObject.SetActive(false);

        // Si tienes un pool, regresa el objeto a él
        Pool.ReturnProjectile(other.gameObject);
        pool.ReturnProjectile(other.gameObject);
    }
    */

}